apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexusdeploy-chart.fullname" . }}
  labels:
    {{- include "nexusdeploy-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexusdeploy-chart.selectorLabels" . | nindent 4 }}
